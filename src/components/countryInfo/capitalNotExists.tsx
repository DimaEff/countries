import React from "react";

import { Typography } from "@mui/material";

const CapitalNotExists = () => {
    return (
        <Typography variant={"h6"} textAlign={"center"} color={"#4f4f4f"} fontWeight={"bold"} margin={3}>
            Capital not exists
        </Typography>
    );
};

export default CapitalNotExists;
